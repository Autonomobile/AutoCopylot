using UnityEngine;
using UnityEditor;

public class CameraHandler : MonoBehaviour {

    //public static UnityEngine.Camera UI_Camera = GameObject.Find("UI Camera").GetComponent(typeof(UnityEngine.Camera)) as UnityEngine.Camera;
    //public static UnityEngine.Camera World_Camera = GameObject.Find("World Camera").GetComponent(typeof(UnityEngine.Camera)) as UnityEngine.Camera;

    //[MenuItem("Cameras/Switch to UI Camera &1")]
    //static void enableUICam() {
    //    UI_Camera.enabled = true;
    //    World_Camera.enabled = false;
    //}

    //[MenuItem("Cameras/Switch to World Camera &2")]
    //static void enableWorldCam() {
    //    UI_Camera.enabled = false;
    //    World_Camera.enabled = true;
    //}
}